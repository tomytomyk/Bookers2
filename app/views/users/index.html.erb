<div class="container">
<div class="row">
<div class="col-lg-3">
  <%= render 'books/list', book: @book, user: @user %>
</div>
<div class="col-lg-9">
<h1>ユーザー一覧</h1>
<table class= "table table-striped">
<thead>
  <tr>
    <th>User image</th>
    <th>user name</th>
  </tr>
</thead>
<tbody>
  <% @users.each do |user| %>
    <tr>
      <td><%= link_to user_path(user.id), class: "user_path(user.id)" do %>
        <%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
          <% end %>
        </td>
      <td><%= user.name %></td>
      <td><%= link_to "Show", user_path(user.id), class: "user_#{user.id}" %></td>
    </tr>
  <% end %>
</tbody>
</table>
</div>
</div>
</div>